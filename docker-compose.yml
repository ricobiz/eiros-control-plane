services:
  eiros:
    build: .
    restart: always
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - XAI_API_KEY=${XAI_API_KEY}
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data